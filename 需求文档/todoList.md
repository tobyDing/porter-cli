## 待办事项

- 项目约定 husky 没有实现，可以参考 [ahooks](./../../ahooks/package.json) 项目配置 husky。
- 同步代码发生冲突时，特别是跨项目同步时，代码冲突，但是报错原因不明确，导致无法按照代码冲突判断失效，无法有效提示用户，需要解决此问题
- 同步完成代码，会直接自动提交代码，这个是不允许的，应该让用户执行 `git add`、`git commit` 手动提交变更到目标项目中。
- 短的 commit-id 也要支持 e3c7118
